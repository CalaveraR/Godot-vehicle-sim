# ARCHITECTURE_CURRENT

- TireRuntimeCoordinator = authoritative pipeline
- TireContactSolver = legacy compatibility
- Rust tire_core = future deterministic backend
- Wheel never applies physics directly
- Suspension updated only via bridge
